import { initRouter } from './router.js';
// Punto de entrada de la aplicación
document.addEventListener('DOMContentLoaded', () => {
    console.log(' Aplicación Blog CRUD iniciada');
    initRouter();
});